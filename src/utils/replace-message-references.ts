/**
 * Placeholder implementation.
 *
 * @param input - Raw message text
 * @returns Same string (no replacement yet)
 */
export function replaceMessageReferences(input: string): string {
  return input;
}
