// This file contains any global test setup that should run before tests

// If we need to add custom matchers or global mocks, we would do it here
// For now, it's an empty file, but it's good to have it set up for future use
